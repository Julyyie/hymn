<% playlists.each do |playlist| %>
    <div data-action="click->games#playlist" uri="<%= playlist.uri %>" data-games-target="card" class="playlist_card text-center">
      <h4><%= playlist.name.truncate(12) %></h4>
      <img src="<%= playlist.images.first["url"] %>">
    </div>
<% end %>
